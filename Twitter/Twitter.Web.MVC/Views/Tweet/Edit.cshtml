@model EditTweetViewModel

@section styles {
    <link href="~/Content/Tweet/create.css" rel="stylesheet" />
}

@using (Html.BeginForm("Edit", "Tweet", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                @Html.ValidationSummary("", new { @class = "text-danger" })
                @Html.TextAreaFor(m => m.Content, new { @class = "tweet-content" })
                <input type="submit" class="btn btn-default pull-right" value="Tweet" />
                @Html.HiddenFor(m => m.PostedOn)
            </div>
        </div>
    </div>
}

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
